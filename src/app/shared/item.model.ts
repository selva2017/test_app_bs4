export class Item {
        constructor(public name: string, public description: string,
                public qty: number, public notes: string) {
                // this.name = name;
                // this.description = description;
                // this.qty = qty;
                // this.notes = notes;
        }
}